{
  "name": "@ht/cms-demo-locales",
  "version": "0.0.0",
  "private": true,
  "description": "Shared locale JSON files for cms-demo apps (admin, resident). Exposed as a workspace package so turbo prune picks them up in Dockerfile builds.",
  "exports": {
    "./*": "./*"
  }
}
