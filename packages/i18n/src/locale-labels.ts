/**
 * Display labels for locale codes. Use for LanguageSelector, Section1557Tagline, etc.
 * Add entries when new languages are enabled so labels stay in sync.
 */
export const LOCALE_LABELS: Record<string, string> = {
  en: 'English',
  es: 'Español',
};
